# Data Directory

This project is designed to run with either local files or S3 URIs.

- `raw/` is for the original credit card default dataset.
- `processed/` is for cleaned and feature-engineered outputs generated by the pipeline.

Do not commit large datasets or sensitive data. In AWS, raw and processed datasets should be stored in S3 and referenced through `configs/config.yaml` or environment variables.

